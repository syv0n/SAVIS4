# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 20.x
        uses: actions/setup-node@v2
        with:
          node-version: '20.x'
      - name: Install dependencies
        run: npm install
        working-directory: ./Savis3
      # - name: Uninstall Firebase Tools
      #   run: npm uninstall -g firebase-tools
      # - name: Install Firebase Tools
      #   run: npm install -g firebase-tools@10.3.0
      - name: Run Dev Server and Tests
        env:
          NODE_OPTIONS: --openssl-legacy-provider
        run: npm run test:ci
        working-directory: ./Savis3
      - name: Build
        env:
          NODE_OPTIONS: --openssl-legacy-provider
        run: npm run build
        working-directory: ./Savis3
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SAVIAS_C1F4D }}'
      #     projectId: savias-c1f4d
      #     entryPoint: './Savis3'
